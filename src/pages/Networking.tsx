import { FC } from 'react';
import CategoryProduct from './CagetoryProduct';

const Networking: FC = () => (
  <CategoryProduct categoryId={18} categoryName="Networking" />
);

export default Networking;
