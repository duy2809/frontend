import { FC } from 'react';
import CategoryProduct from './CagetoryProduct';

const GamingConsole: FC = () => (
  <CategoryProduct categoryId={14} categoryName="Gaming console" />
);

export default GamingConsole;
